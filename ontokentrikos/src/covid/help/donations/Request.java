package covid.help.donations;

import covid.help.items.Entity;

public class Request extends RequestDonation{
    public Request(Entity entity, long quantity) {
        super(entity, quantity);
    }
}
